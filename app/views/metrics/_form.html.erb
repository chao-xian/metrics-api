<%= form_with(model: metric) do |form| %>
  <% if metric.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(metric.errors.count, "error") %> prohibited this metric from being saved:</h2>

      <ul>
        <% metric.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :project, "Project: " %>
    <%= form.collection_select :project_ids, Project.all, :id, :name %>
  </div>

  <div>
    <%= form.label :name, "Name: " %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :prefix, "Prefix: " %>
    <%= form.text_field :prefix %>
  </div>

  <div>
    <%= form.label :number, "Number: " %>
    <%= form.text_field :number %>
  </div>

  <div>
    <%= form.label :amount, "Amount: " %>
    <%= form.text_field :amount %>
  </div>

  <div>
    <%= form.label :label, "Label: " %>
    <%= form.text_field :label %>
  </div>

  <div>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
